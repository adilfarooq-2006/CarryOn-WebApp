/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i0.wp.com', 'avatars.githubusercontent.com', 'example.com'], // Add the domains you trust
    },
  }
  
  export default nextConfig;
  